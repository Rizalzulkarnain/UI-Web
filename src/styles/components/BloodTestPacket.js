import styled from 'styled-components';

import { theme } from '../root';

export const BloodTestPacketContainer = styled.div`
  .separate__main {
    border: solid 1px ${theme.colors.primary};
  }

  margin-bottom: 60px;
`;
